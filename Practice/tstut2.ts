interface Student {
    id : number;
    Name : string;
}
var register: Student = { id : 101, Name : "Darhan" }
console.log("Register number " + register.id + " is " + register.Name);

// initializing values to variable using interface type